
{
  "name": "twig-static-watch",
  "version": "1.0.0",
  "scripts": {
    "tailwind": "tailwindcss -i ./tailwind/input.css -o ./assets/style.css --watch",
    "dev": "npx nodemon --watch templates --watch data --ext twig,json --exec \"php render.php --force\"",

    "serve": "browser-sync start --server build --files \"build/**/*.html\" \"build/assets/**/*.css\"",

    "start": "concurrently \"npm run tailwind\" \"npm run dev\" \"npm run serve\""
  },
  "devDependencies": {
    "browser-sync": "^2.27.0",
    "nodemon": "^2.0.20",
    "concurrently": "^7.6.0",
    "tailwindcss": "^3.3.0"
  }
}
